{

    "version": "0.2.0",
    "configurations": [
        {
            "type": "aws-sam",
            "request": "direct-invoke",
            "name": "Invoke Local Lambda Function",
            "invokeTarget": {
                "target": "template",
                "templatePath": "path/to/template.yaml",
                "logicalName": "FunctionLogicalId"
            }
        }
    ]
}

